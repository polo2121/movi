import React, { useState } from 'react'

const useControlSlide = (props) => {

    console.log(props)
    const [show, setShow] = useState("")

    return (
        <div>useControlSlide</div>
    )
}

export default useControlSlide